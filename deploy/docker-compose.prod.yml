services:
  backend:
    image: ${DOCKER_USERNAME}/kiyim-chechak-backend:latest
    container_name: kiyim-chechak-backend
    restart: unless-stopped
    env_file:
      - .env
    expose:
      - "3000"
    networks:
      - kiyim-net
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3000/api/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 40s

  frontend:
    image: ${DOCKER_USERNAME}/kiyim-chechak-frontend:latest
    container_name: kiyim-chechak-frontend
    restart: unless-stopped
    ports:
      - "127.0.0.1:8080:80"
    depends_on:
      backend:
        condition: service_healthy
    networks:
      - kiyim-net

networks:
  kiyim-net:
    driver: bridge
